# Root env — used by docker compose.
# Backend-specific settings live in portfolio-backend/.env.example
# Frontend-specific settings live in portfolio-frontend/src/environments/*.ts

CORS_ORIGINS="http://localhost:8080,http://localhost:4200"

# Phase 2 — Supabase persistence
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
