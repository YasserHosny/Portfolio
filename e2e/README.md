# End-to-end tests

Placeholder for Playwright / Cypress E2E suites.

Suggested Phase-2 test areas:

- Homepage loads hero + snapshot + all sections
- Product Lab route lists 6 items
- Case Studies route lists 5 items
- Contact form validation and success flow
- Header navigation across all routes
- Mobile menu open/close

Nothing runs here yet — the CI job in `.github/workflows/ci.yml` only builds
the frontend and runs backend pytest today.
